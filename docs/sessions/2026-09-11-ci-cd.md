# Session 2026-09-11 — Tích hợp CI/CD

## Đã làm

- Nâng `.github/workflows/ci.yml`:
  - Trigger push: `main` + `bootstrap/base`
  - Job `static`: ruff check + ruff format --check
  - Job `schema`: validate_repo (tách khỏi static)
  - Job `unit`: matrix OS × Python 3.11/3.12
  - Job `quality` cần 4 cổng con xanh
- Thêm `.github/workflows/cd.yml` placeholder (dry-run, chờ ADR trước khi publish)
- Makefile + pyproject (ruff format) + CONTRIBUTING + CHANGELOG

## Chưa làm

- mypy strict (đợi có `src/`)
- Dependabot / pin update tự động
- Deploy thật (cấm cho tới ADR)

## Ghi chú

CD không side-effect. Mọi publish phải qua environment protection + approval.
