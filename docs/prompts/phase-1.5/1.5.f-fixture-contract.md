# Subtask 1.5.f — Fixture example + contract test

**Copy toàn bộ khối dưới đây cho ĐÚNG MỘT subagent.**

---

```text
Bạn là subagent Sales-Hunter. Chỉ làm subtask 1.5.f.

## Metadata
subtask_id: 1.5.f
parent_phase: 1.5
parent_task_pack: 0003-phase1.5-publication-candidate
model_tier: T1
external_side_effects: none

## Mục tiêu
Thêm fixture publication-candidate hợp lệ (synthetic) và contract test: builder output (hoặc fixture) validate schema.

## DoD
- [ ] File dưới schemas/examples/valid/ (hoặc đúng convention repo đang dùng cho examples)
- [ ] Contract test đọc schema + example / output builder
- [ ] Không có dữ liệu thương mại/PII thật
- [ ] unittest + validate_repo liên quan xanh

## Phạm vi
Được chạm: schemas/examples/, tests/, tối thiểu src/ nếu cần export helper test
Không được chạm: logic ranking/Money, adapters mạng

## Phải đọc
1. AGENTS.md
2. schemas/publication-candidate.v1.json
3. schemas/examples/ hiện có (pattern)
4. tools/validate_repo.py (nếu liên quan examples)

## Cấm
- Payload thật từ Shopee/TikTok
- Secret

## Kiểm
uv run python -m unittest discover -s tests -v
uv run python tools/validate_repo.py

## Báo cáo trả orchestrator
### Kết quả subtask 1.5.f
- Trạng thái:
- Fixture path:
- Lệnh + kết quả:
- Phát hiện ngoài phạm vi:
- Rủi ro:
```
