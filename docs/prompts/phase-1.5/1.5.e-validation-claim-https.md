# Subtask 1.5.e — Validation schema + HTTPS + claim_snapshot invariants

**Copy toàn bộ khối dưới đây cho ĐÚNG MỘT subagent.**

---

```text
Bạn là subagent S-N Sales. Chỉ làm subtask 1.5.e.

## Metadata
subtask_id: 1.5.e
parent_phase: 1.5
parent_task_pack: 0003-phase1.5-publication-candidate
model_tier: T2
external_side_effects: none

## Mục tiêu
Siết validation sau build: JSON Schema publication-candidate.v1, affiliate_url HTTPS, claim_snapshot khớp observation, reject input không hợp lệ bằng exception rõ ràng.

## DoD
- [ ] Output hợp lệ pass Draft202012Validator với schemas/publication-candidate.v1.json
- [ ] affiliate_url không https → lỗi
- [ ] approval.draft_sha256 luôn khớp draft_sha256
- [ ] Test HTTPS / schema / claim xanh

## Phạm vi
Được chạm: src/s_n_sales/pipeline/, tests/
Không được chạm: adapters, publish, đổi semantics schema v1 (chỉ consume schema)

## Phải đọc
1. AGENTS.md
2. schemas/publication-candidate.v1.json
3. src/s_n_sales/pipeline/draft.py (pattern validate observation)
4. docs/impl/PHASE-1.5-IMPLEMENTATION.md mục 4.5

## Cấm
- Sửa file schema để “cho dễ pass”
- Side effect mạng

## Kiểm
uv run python -m unittest discover -s tests -v
uv run python tools/validate_repo.py

## Báo cáo trả orchestrator
### Kết quả subtask 1.5.e
- Trạng thái:
- Diff chính:
- Lệnh + kết quả:
- Phát hiện ngoài phạm vi:
- Rủi ro:
```
