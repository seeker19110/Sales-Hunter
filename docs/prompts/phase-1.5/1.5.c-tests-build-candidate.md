# Subtask 1.5.c — Test đỏ build_publication_candidate (happy path + biên cơ bản)

**Copy toàn bộ khối dưới đây cho ĐÚNG MỘT subagent.**

---

```text
Bạn là subagent S-N Sales. Chỉ làm subtask 1.5.c — viết test, chưa implement builder đầy đủ.

## Metadata
subtask_id: 1.5.c
parent_phase: 1.5
parent_task_pack: 0003-phase1.5-publication-candidate
model_tier: T1
external_side_effects: none

## Mục tiêu
Viết unit test cho build_publication_candidate theo PHASE-1.5-IMPLEMENTATION.md mục 4, 6.1.

## DoD
- [ ] test_build_sets_pending_approval
- [ ] test_build_uses_default_disclosure_when_none
- [ ] test_build_rejects_empty_content (hoặc tương đương)
- [ ] test_build_claim_snapshot_matches_observation
- [ ] test_build_requires_https_affiliate_url (có thể đỏ đến khi 1.5.e)
- [ ] File test trong tests/

## Phạm vi
Được chạm: tests/
Không được chạm: src/ (impl là 1.5.d/e)

## Phải đọc
1. AGENTS.md
2. docs/impl/PHASE-1.5-IMPLEMENTATION.md
3. schemas/publication-candidate.v1.json
4. compute_draft_sha256 đã có (1.5.b)

## Cấm
- Implement builder trong src/
- Mạng, publish, sửa schema

## Kiểm
uv run python -m unittest discover -s tests -v

## Báo cáo trả orchestrator
### Kết quả subtask 1.5.c
- Trạng thái:
- File test:
- Lệnh + kết quả:
- Phát hiện ngoài phạm vi:
- Rủi ro:
```
