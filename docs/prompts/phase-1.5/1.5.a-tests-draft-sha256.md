# Subtask 1.5.a — Test đỏ cho compute_draft_sha256

**Copy toàn bộ khối dưới đây cho ĐÚNG MỘT subagent.**

---

```text
Bạn là subagent S-N Sales. Chỉ làm subtask 1.5.a. Không làm 1.5.b hay phần implement production.

## Metadata
subtask_id: 1.5.a
parent_phase: 1.5
parent_task_pack: 0003-phase1.5-publication-candidate
model_tier: T1
external_side_effects: none

## Mục tiêu
Viết unit test (đỏ) cho hàm compute_draft_sha256 theo docs/impl/PHASE-1.5-IMPLEMENTATION.md mục 4.3 và 6.1 — CHƯA implement hàm production (test được fail vì thiếu hàm hoặc fail đúng assertion là chấp nhận ở bước này nếu TDD nghiêm: ưu tiên test tồn tại và mô tả hành vi).

## DoD
- [ ] Có file test (ví dụ tests/test_draft_sha256.py hoặc tương đương trong tests/)
- [ ] test_compute_draft_sha256_stable — cùng input → cùng hash
- [ ] test_compute_draft_sha256_changes_on_content
- [ ] test_compute_draft_sha256_changes_on_disclosure
- [ ] test_compute_draft_sha256_changes_on_claim_snapshot
- [ ] Import/API test giả định đúng chữ ký trong PHASE-1.5-IMPLEMENTATION.md

## Phạm vi
Được chạm: tests/ only
Không được chạm: src/, schemas/, docs/ (trừ khi thiếu __init__ test bắt buộc — vẫn ưu tiên chỉ tests/)

## Phải đọc
1. AGENTS.md
2. docs/SUBAGENT-TASK-CONVENTION.md
3. docs/impl/PHASE-1.5-IMPLEMENTATION.md (mục 4.3, 5, 6)
4. schemas/publication-candidate.v1.json (hiểu draft_sha256 pattern)

## Ràng buộc
- TDD: viết test trước impl
- Canonical: sort_keys=True, separators=(",", ":"), UTF-8, SHA-256 hex 64 ký tự
- Side effect: none

## Cấm
- Implement compute_draft_sha256 trong src/ (đó là 1.5.b)
- Publish, mạng, sửa schema semantics
- Làm subtask khác

## Kiểm
uv run python -m unittest discover -s tests -v
(Ghi nhận test fail vì chưa có impl là OK nếu test đã đúng hành vi.)

## Báo cáo trả orchestrator
### Kết quả subtask 1.5.a
- Trạng thái:
- File test:
- Lệnh + kết quả:
- Phát hiện ngoài phạm vi:
- Rủi ro:
```
