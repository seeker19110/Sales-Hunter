# Subtask 1.5.b — Implement compute_draft_sha256

**Copy toàn bộ khối dưới đây cho ĐÚNG MỘT subagent.**

---

```text
Bạn là subagent S-N Sales. Chỉ làm subtask 1.5.b.

## Metadata
subtask_id: 1.5.b
parent_phase: 1.5
parent_task_pack: 0003-phase1.5-publication-candidate
model_tier: T2
external_side_effects: none

## Mục tiêu
Implement hàm compute_draft_sha256 (pure) để các test 1.5.a xanh, đúng thuật toán trong PHASE-1.5-IMPLEMENTATION.md mục 4.3.

## DoD
- [ ] Hàm nằm trong src/s_n_sales/pipeline/ (publication.py mới hoặc module rõ ràng)
- [ ] Cùng input → cùng hash; đổi content/disclosure/claim_snapshot/url/channel/observation_id → hash đổi
- [ ] Không gồm publication_id, approval, idempotency_key trong canonical payload
- [ ] Test 1.5.a xanh

## Phạm vi
Được chạm: src/s_n_sales/pipeline/, tests/ (chỉ nếu cần sửa import)
Không được chạm: adapters mạng, schema breaking, docs lớn, build_publication_candidate đầy đủ (1.5.d+)

## Phải đọc
1. AGENTS.md
2. docs/impl/PHASE-1.5-IMPLEMENTATION.md mục 4.3 và 5
3. Test file từ 1.5.a
4. src/s_n_sales/pipeline/draft.py (tham khảo style)

## Ràng buộc
- Pure function, không I/O mạng
- json.dumps(..., ensure_ascii=False, sort_keys=True, separators=(",", ":"))
- hashlib.sha256(...).hexdigest()

## Cấm
- Làm builder publication-candidate đầy đủ
- Side effect ngoài none

## Kiểm
uv run python -m unittest discover -s tests -v
# các test sha256 phải xanh

## Báo cáo trả orchestrator
### Kết quả subtask 1.5.b
- Trạng thái:
- File impl:
- Lệnh + kết quả:
- Phát hiện ngoài phạm vi:
- Rủi ro:
```
