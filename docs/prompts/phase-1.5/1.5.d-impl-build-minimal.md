# Subtask 1.5.d — Implement build_publication_candidate tối thiểu + disclosure mặc định

**Copy toàn bộ khối dưới đây cho ĐÚNG MỘT subagent.**

---

```text
Bạn là subagent S-N Sales. Chỉ làm subtask 1.5.d.

## Metadata
subtask_id: 1.5.d
parent_phase: 1.5
parent_task_pack: 0003-phase1.5-publication-candidate
model_tier: T2
external_side_effects: none

## Mục tiêu
Implement build_publication_candidate tối thiểu: content, affiliate_url, target_channel, default disclosure, claim_snapshot từ observation, draft_sha256 qua compute_draft_sha256, approval.status=pending, idempotency_key ổn định.

## DoD
- [ ] Hàm public theo chữ ký PHASE-1.5-IMPLEMENTATION.md mục 5
- [ ] Disclosure không rỗng; None/rỗng → template mặc định
- [ ] approval.status == "pending" và approval.draft_sha256 == draft_sha256
- [ ] Các test happy path 1.5.c liên quan xanh (trừ phần có thể để 1.5.e)

## Phạm vi
Được chạm: src/s_n_sales/pipeline/, tests/ (sửa import/fixture nhỏ)
Không được chạm: adapters mạng, publish, UI, schema breaking

## Phải đọc
1. AGENTS.md
2. docs/impl/PHASE-1.5-IMPLEMENTATION.md mục 4–5
3. schemas/publication-candidate.v1.json
4. compute_draft_sha256 hiện có

## Ràng buộc
- claim_snapshot lấy từ observation (không tin input ngoài)
- Không float tiền
- Side effect: none

## Cấm
- Tắt disclosure
- Gọi LLM / mạng
- Làm 1.5.f docs

## Kiểm
uv run python -m unittest discover -s tests -v

## Báo cáo trả orchestrator
### Kết quả subtask 1.5.d
- Trạng thái:
- File impl:
- Lệnh + kết quả:
- Phát hiện ngoài phạm vi:
- Rủi ro:
```
